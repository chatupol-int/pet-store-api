name: Build
on:
  push:
    branches: [ main, dev ]

jobs:
  test-on-ubuntu:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Hello from Gitea Actions!"

  test-on-self-hosted:
    runs-on: self-hosted
    steps:
      - run: echo "on tiger hosted"
      - run: pwd
      - run: ls -la